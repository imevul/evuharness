# Maintainer-only notes. This directory is its own git repository so the notes
# keep history and `git status` without ever entering the publishable tree.
/docs/internal/

# Secrets and local overrides — never commit
.env
.env.*
!.env.example
*.local

# Local runtime state (SQLite stores, demo data, native dev logs/pids)
/data/
/.data/
*.sqlite
*.sqlite3
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
*.sqlite3-wal
*.sqlite3-shm

# Node
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo
.vite/

# Testing / tooling caches
coverage/
.vitest/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
